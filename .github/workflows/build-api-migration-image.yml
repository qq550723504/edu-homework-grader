name: Build API migration image

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: Immutable 40-character source commit SHA containing the Alembic history.
        required: true
        type: string
  pull_request:
    types: [labeled]

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.action == 'labeled' &&
       github.event.label.name == 'build-migration-image' &&
       github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ubuntu-latest
    steps:
      - name: Resolve and validate source SHA
        shell: bash
        env:
          WORKFLOW_SOURCE_SHA: ${{ inputs.source_sha }}
          PULL_REQUEST_SOURCE_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          SOURCE_SHA="$WORKFLOW_SOURCE_SHA"
          if [[ "$GITHUB_EVENT_NAME" == 'pull_request' ]]; then
            SOURCE_SHA="$PULL_REQUEST_SOURCE_SHA"
          fi
          if [[ "$SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            printf 'SOURCE_SHA=%s\n' "$SOURCE_SHA" >> "$GITHUB_ENV"
            exit 0
          fi
          echo 'source_sha must be a 40-character lower-case Git SHA.' >&2
          exit 1

      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ env.SOURCE_SHA }}

      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        id: build
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/edu-homework-grader-api:${{ env.SOURCE_SHA }}

      - name: Record immutable image digest
        shell: bash
        env:
          IMAGE_REPOSITORY: ghcr.io/${{ github.repository_owner }}/edu-homework-grader-api
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          if [[ ! "$IMAGE_DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo 'Build did not return a valid SHA-256 image digest.' >&2
            exit 1
          fi
          printf '%s\n' "$IMAGE_DIGEST" > api-migration-image-digest.txt
          printf 'Migration image: `%s@%s`\n' "$IMAGE_REPOSITORY" "$IMAGE_DIGEST" >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: api-migration-image-digest
          path: api-migration-image-digest.txt
          if-no-files-found: error
          retention-days: 1
