# ADR-0001：第一阶段采用单体仓库

- 状态：已接受
- 日期：2026-07-18

## 决策

第一阶段将 Web、核心 API、批改服务和文档放在同一仓库中，但保持独立部署单元。

## 原因

- 产品与评分规则会快速协同迭代；
- 跨端接口变更可以在一个 PR 中完成；
- 统一 CI、文档和 Issue 管理；
- 仍可通过目录、包和容器边界保持服务独立性。

## 后果

需要严格设置 CODEOWNERS、目录级测试和接口契约。业务规模扩大后，可将高负载模型服务拆分到独立仓库。
